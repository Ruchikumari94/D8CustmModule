<!-- Add you custom twig html here -->
{#
/**
* @file
* Theme implementation of the store map.
*
* @ingroup themeable
*/
#}

<p>{{ description }}</p>
<div id="resume" style="width:500px; height:500px;"></div>

{# if ($resume) {
 foreach ($resume as $fields) {
   $fields[] =[
     'candidate_name' => $fields->candidate_name,
     'candidate_mail' => $fields->candidate_mail,
     'candidate_number' => $fields->candidate_number,
     'candidate_dob' => $fields->candidate_dob,
     'candidate_gender' => $fields->candidate_gender,
     'candidate_confirmation' => $fields->candidate_confirmation,
     'candidate_copy' => $fields->candidate_copy
   ];
 }
}
return $fields; #}

<p>Test twig template!</p>
 
<p>test_var: {{ test_var }}</p>